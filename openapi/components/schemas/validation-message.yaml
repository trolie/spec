type: object
additionalProperties: false
properties:
  level:
    type: string
    enum:
      - warning
      - error
    example: error
  message:
    type: string
    pattern: ^(.){1,500}$
    maxLength: 500
    example: Data point XXX is out of range
