type: object
description: >

  Used as a base type for mutable ratings proposals, i.e., forecast and seasonal
  ratings.  

properties:
  last-updated:
    $ref: ./timestamp.yaml
  ratings-provider:
    $ref: ./entity-id.yaml
