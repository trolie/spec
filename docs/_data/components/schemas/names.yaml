type: object
properties:
  resource-id:
    $ref: ./generic-identifier.yaml
  alternate-identifiers:
    type: array
    maxItems: 10
    minItems: 1
    items:
      type: object
      properties:
        name:
          $ref: ./generic-identifier.yaml
        authority:
          $ref: ./entity-id.yaml
        mrid:
          $ref: ./generic-identifier.yaml
      required:
        - name
required:
  - resource-id
